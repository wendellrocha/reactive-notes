{
  "name": "reactive-notes",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --driver-package meteortesting:mocha"
  },
  "dependencies": {
    "@babel/runtime": "^7.3.4",
    "bcrypt": "^5.0.0",
    "framework7": "^4.5.1",
    "meteor-node-stubs": "^0.4.1",
    "simpl-schema": "^1.5.5"
  },
  "devDependencies": {
    "chai": "^4.1.2"
  }
}
